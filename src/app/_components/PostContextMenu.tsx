export default function PostContextMenu() {
  // const cursorX = window.page || 0;
  // const cursorY = window.event?.clientY || 0;

  return <div className={`fixed left-[1/2] top-[1/2]`}>CONTEXT MENU HERE</div>;
}
